# Go Server Example

This example runs a TCP server that logs received lines.

## Run

```bash
go run main.go
```

## Requirements
- Go (https://golang.org/dl/)

## Usage
Start the server before running the C++ client (see cpp_to_go_client_README.md).
